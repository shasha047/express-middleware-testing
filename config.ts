export const __root = __dirname;
export const node_port: number = 2020;
export let checkCounter: Array<number> = [Math.floor(Math.random()*10)];
export let randomSubtractor: Array<number> = [Math.floor(Math.random()*20)]
export const statusCode = {
    success:200,
    failure:500
}